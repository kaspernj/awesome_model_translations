before_script:
  - cd spec/dummy && RAILS_ENV=test bundle exec rails db:create db:environment:set db:schema:load
rvm: true
builds:
  build_1:
    environment:
      RUBY_VERSION: 3.1.2
    name: RSpec
    script:
      - bundle exec rspec
